@* @model HealthWellbeing.ViewModels.RPaginationInfo<HealthWellbeingRoom.Models.Reservation>
 *@
@{
    ViewData["Title"] = "Reservas de Salas";
}

<h2 class="text-primary mb-4">
    <i class="bi bi-calendar-check me-2"></i> @ViewData["Title"]
</h2>

<!-- Botão de pesquisa -->
<div class="d-flex justify-content-end mb-3">
    <button class="btn btn-outline-primary"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#reservationSearch"
            aria-expanded="false"
            aria-controls="reservationSearch">
        <i class="bi bi-search me-1"></i> Pesquisa
    </button>
</div>

<!-- FORMULÁRIO DE PESQUISA (collapse) -->
@* <div class="collapse mb-3" id="reservationSearch">
    <div class="card card-body">
        <form asp-action="Reservations" method="get" class="row g-2 align-items-end">
            <input type="hidden" name="id" value="@ViewBag.RoomId" />

            <!-- Primeira linha: Id da Reserva e Utilizador -->
            <div class="col-md-6">
                <label for="searchReservationId" class="form-label">Id da Reserva</label>
                <input id="searchReservationId"
                       name="searchReservationId"
                       value="@ViewBag.SearchReservationId"
                       class="form-control"
                       placeholder="Pesquisar pelo Id da reserva"
                       type="number"
                       min="1" />
            </div>

            <div class="col-md-6">
                <label for="searchUser" class="form-label">Utilizador</label>
                <input id="searchUser"
                       name="searchUser"
                       value="@ViewBag.SearchUser"
                       class="form-control"
                       placeholder="Pesquisar pelo utilizador"
                       type="search" />
            </div>

            <!-- Segunda linha: Intervalo de datas -->
            <div class="col-md-6">
                <label for="searchStartDate" class="form-label">Data Início</label>
                <input id="searchStartDate"
                       name="searchStartDate"
                       value="@ViewBag.SearchStartDate"
                       class="form-control"
                       type="date" />
            </div>

            <div class="col-md-6">
                <label for="searchEndDate" class="form-label">Data Fim</label>
                <input id="searchEndDate"
                       name="searchEndDate"
                       value="@ViewBag.SearchEndDate"
                       class="form-control"
                       type="date" />
            </div>

            <!-- Botões -->
            <div class="col-12 mt-2 d-flex gap-2">
                <input type="hidden" name="page" value="1" />
                <button type="submit" class="btn btn-outline-primary flex-fill">
                    <i class="bi bi-search me-1"></i> Pesquisa
                </button>
                <a asp-action="Reservations" asp-route-id="@ViewBag.RoomId" class="btn btn-secondary flex-fill">
                    <i class="bi bi-x-circle me-1"></i> Limpar
                </a>
            </div>
        </form>
    </div>
</div> *@

<div class="table-responsive">
    <table class="table table-bordered table-hover align-middle">
        <thead class="table-light">
            <tr>
                <th>Id da Reserva</th>
                <th>Data Início</th>
                <th>Data Fim</th>
                <th>Utilizador</th>
                <th>Descrição</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
@*             @foreach (var reserva in Model.Items)
            {
                <tr>
                    <td>@reserva.ReservationId.ToString("D3")</td>
                    <td>@reserva.StartDate.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@reserva.EndDate.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@reserva.User?.UserName</td>
                    <td>@reserva.Description</td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@reserva.ReservationId" class="btn btn-sm btn-warning me-1">
                            <i class="bi bi-pencil-square"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@reserva.ReservationId" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            } *@
        </tbody>
    </table>
</div>

<!-- Botões de navegação -->
<div class="mt-4 d-flex justify-content-end gap-2">
    <a asp-action="Details" asp-route-id="@ViewBag.RoomId" class="btn btn-primary">
        <i class="bi bi-arrow-left-circle me-1"></i> Voltar à Sala
    </a>

    <a asp-action="Index" class="btn btn-primary">
        <i class="bi bi-list-ul me-1"></i> Voltar à Lista
    </a>
</div>

<!-- PAGINAÇÃO AO FINAL -->
@* <nav aria-label="Reservation page" class="d-flex justify-content-between align-items-center mt-4">
    @if (Model.TotalItems > 0)
    {
        <span class="text-muted">
            Mostrando
            <strong>@((Model.CurrentPage - 1) * Model.ItemsPerPage + 1)</strong>
            -
            <strong>@(Math.Min(Model.CurrentPage * Model.ItemsPerPage, Model.TotalItems))</strong>
            de <strong>@Model.TotalItems</strong>
        </span>
    }
    else
    {
        <span class="text-muted">0 itens</span>
    }

    <ul class="pagination mb-0">
        <!-- Primeira página -->
        @if (Model.CurrentPage == 1)
        {
            <li class="page-item disabled">
                <a class="page-link"><i class="bi bi-chevron-double-left"></i> Primeira</a>
            </li>
        }
        else
        {
            <li class="page-item">
                <a class="page-link"
                   asp-action="Reservations"
                   asp-route-id="@ViewBag.RoomId"
                   asp-route-page="1">
                    <i class="bi bi-chevron-double-left"></i> Primeira
                </a>
            </li>
        }

        <!-- Números de página -->
        @for (int p = Model.FirstPageShow; p <= Model.LastPageShow; p++)
        {
            if (p == Model.CurrentPage)
            {
                <li class="page-item active">
                    <a class="page-link" href="#">@p</a>
                </li>
            }
            else
            {
                <li class="page-item">
                    <a class="page-link"
                       asp-action="Reservations"
                       asp-route-id="@ViewBag.RoomId"
                       asp-route-page="@p">
                        @p
                    </a>
                </li>
            }
        }

        <!-- Última página -->
        @if (Model.CurrentPage == Model.TotalPages)
        {
            <li class="page-item disabled">
                <a class="page-link">Última <i class="bi bi-chevron-double-right"></i></a>
            </li>
        }
        else
        {
            <li class="page-item">
                <a class="page-link"
                   asp-action="Reservations"
                   asp-route-id="@ViewBag.RoomId"
                   asp-route-page="@Model.TotalPages">
                    Última <i class="bi bi-chevron-double-right"></i>
                </a>
            </li>
        }
    </ul>
</nav> *@