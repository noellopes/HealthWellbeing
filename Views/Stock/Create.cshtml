@model HealthWellbeing.Models.Stock

@{
    ViewData["Title"] = "Criar Stock";
}

<h2 class="text-primary fw-bold mb-4">➕ Criar Stock</h2>

<form asp-action="Create">
    <div class="mb-3">
        <label class="form-label">Consumível</label>
        <select asp-for="ConsumivelID" class="form-select" required>
            <option value="">-- Escolha o Consumível --</option>
            @foreach (var c in ViewBag.Consumiveis)
            {
                <option value="@c.Id">@c.Nome</option>
            }
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Zona de Armazenamento</label>
        <select asp-for="ZonaID" class="form-select" required>
            <option value="">-- Escolha a Zona --</option>
            @foreach (var z in ViewBag.Zonas)
            {
                <option value="@z.Id">@z.Nome</option>
            }
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Quantidade Atual</label>
        <input asp-for="QuantidadeAtual" class="form-control" type="number" required />
    </div>

    <div class="mb-3">
        <label class="form-label">Quantidade Mínima</label>
        <input asp-for="QuantidadeMinima" class="form-control" type="number" required />
    </div>

    <div class="mb-3">
        <label class="form-label">Quantidade Máxima</label>
        <input asp-for="QuantidadeMaxima" class="form-control" type="number" required />
    </div>

    <button type="submit" class="btn btn-success px-4">Criar</button>
    <a asp-action="Index" class="btn btn-secondary px-4">Voltar</a>
</form>

