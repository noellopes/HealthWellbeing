@model HealthWellbeing.Models.ExameTipo

@{
    ViewData["Title"] = "Apagar Tipo de Exame";
}

<div class="container mt-4">

    <div class="row justify-content-center">
        <div class="col-lg-7 col-md-9">

            <div class="card border-danger shadow-lg">

                <div class="card-header bg-danger text-white">
                    <h1 class="h4 mb-0">⚠️ Apagar Tipo de Exame</h1>
                </div>

                <div class="card-body">
                    <h3 class="card-title text-danger mb-4">Tem certeza de que deseja apagar este tipo de exame?</h3>
                    <p class="card-text">A exclusão deste item é **irreversível**.</p>

                    <hr class="my-3" />

                    <dl class="row mb-0">
                        <dt class="col-sm-4 text-muted">
                            @Html.DisplayNameFor(model => model.Nome)
                        </dt>
                        <dd class="col-sm-8 fw-bold">
                            @Html.DisplayFor(model => model.Nome)
                        </dd>

                        <dt class="col-sm-4 text-muted">
                            @Html.DisplayNameFor(model => model.Descricao)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.Descricao)
                        </dd>

                        <dt class="col-sm-4 text-muted">
                            @Html.DisplayNameFor(model => model.Especialidade!.Nome)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.Especialidade!.Nome)
                        </dd>
                    </dl>
                </div>

                <div class="card-footer d-flex justify-content-between">
                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="ExameTipoId" />
                        <button type="submit" value="Delete" class="btn btn-danger shadow-sm">
                            <i class="bi bi-trash"></i> Sim, Apagar
                        </button>
                    </form>

                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </a>
                </div>
            </div>

        </div>
    </div>
</div>
