@model HealthWellbeing.Models.Genero

@{
    ViewData["Title"] = "Criar Gênero";
}

<h1>@ViewData["Title"]</h1>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

<form asp-action="Create" method="post">
    @Html.AntiForgeryToken()

    <div asp-validation-summary="All" class="text-danger"></div>

    <!-- Nome do Gênero -->
    <div class="mb-3">
        <label asp-for="NomeGenero" class="form-label fw-bold">Nome do Gênero *</label>
        <input asp-for="NomeGenero" class="form-control" placeholder="Ex: Masculino, Feminino, Outro" />
        <span asp-validation-for="NomeGenero" class="text-danger small"></span>
    </div>

    <!-- Botões -->
    <div class="mt-4">
        <button type="submit" class="btn btn-success me-2">
            <i class="fas fa-save"></i> Criar Gênero
        </button>

        <a asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Voltar à Lista
        </a>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
