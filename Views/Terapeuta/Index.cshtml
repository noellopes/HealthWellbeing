@model IEnumerable<HealthWellbeing.Models.TerapeutaModel>

@{
    ViewData["Title"] = "Lista de Terapeutas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Lista de Terapeutas</h1>
<hr />

@* Mensagem de sucesso *@
@if (ViewBag.SuccessMessage != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @ViewBag.SuccessMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
    </div>
}

@* Botão para criar novo terapeuta *@
<p>
    <a asp-action="Create" class="btn btn-success">Adicionar Novo Terapeuta</a>
</p>

@* FORMULÁRIO DE PESQUISA *@
<div class="card mb-4">
    <div class="card-body">
        <form asp-action="Index" method="get">
            <div class="row">

                @* Filtro por Nome *@
                <div class="col-md-4">
                    <label class="form-label">Nome</label>
                    <input type="text"
                           name="nome"
                           value="@ViewBag.NomeFiltro"
                           class="form-control"
                           placeholder="Pesquisar por nome..." />
                </div>

                @* Filtro por Especialidade *@
                <div class="col-md-4">
                    <label class="form-label">Especialidade</label>
                    <input type="text"
                           name="especialidade"
                           value="@ViewBag.EspecialidadeFiltro"
                           class="form-control"
                           placeholder="Pesquisar por especialidade..." />
                </div>

                @* Filtro por Ativo *@
                <div class="col-md-3">
                    <label class="form-label">Estado</label>
                    <select name="ativo" class="form-select">
                        <option value="">Todos</option>

                        <option value="true"
                                selected="@(ViewBag.AtivoFiltro != null && ViewBag.AtivoFiltro == true)">
                            Ativo
                        </option>

                        <option value="false"
                                selected="@(ViewBag.AtivoFiltro != null && ViewBag.AtivoFiltro == false)">
                            Inativo
                        </option>
                    </select>
                </div>

                @* Botão Filtrar *@
                <div class="col-md-1 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary w-100">Filtrar</button>
                </div>
            </div>
        </form>
    </div>
</div>

@* Tabela que lista todos os terapeutas *@
<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nome)</th>
            <th>@Html.DisplayNameFor(model => model.Especialidade)</th>
            <th>@Html.DisplayNameFor(model => model.Telefone)</th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th>@Html.DisplayNameFor(model => model.AnosExperiencia)</th>
            <th>@Html.DisplayNameFor(model => model.Ativo)</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Nome)</td>
                <td>@Html.DisplayFor(modelItem => item.Especialidade)</td>
                <td>@Html.DisplayFor(modelItem => item.Telefone)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.AnosExperiencia)</td>
                <td>
                    @if (item.Ativo)
                    {
                        <span class="badge bg-success">Ativo</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Inativo</span>
                    }
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.TerapeutaId" class="btn btn-info btn-sm">Detalhes</a>
                    <a asp-action="Edit" asp-route-id="@item.TerapeutaId" class="btn btn-primary btn-sm">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.TerapeutaId" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@* Paginação *@
@{
    int paginaAtual = ViewBag.Page ?? 1;
    int totalPaginas = ViewBag.TotalPaginas ?? 1;
}

@if (totalPaginas > 1)
{
    <nav aria-label="Paginação de terapeutas">
        <ul class="pagination justify-content-center">

            @* Primeira página *@
            <li class="page-item @(paginaAtual == 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-page="1"
                   asp-route-nome="@ViewBag.NomeFiltro"
                   asp-route-especialidade="@ViewBag.EspecialidadeFiltro"
                   asp-route-ativo="@ViewBag.AtivoFiltro">«</a>
            </li>

            @* Intervalo dinâmico *@
            @{
                int startPage = Math.Max(1, paginaAtual - 3);
                int endPage = Math.Min(totalPaginas, paginaAtual + 3);
            }

            @for (int i = startPage; i <= endPage; i++)
            {
                <li class="page-item @(i == paginaAtual ? "active" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-page="@i"
                       asp-route-nome="@ViewBag.NomeFiltro"
                       asp-route-especialidade="@ViewBag.EspecialidadeFiltro"
                       asp-route-ativo="@ViewBag.AtivoFiltro">@i</a>
                </li>
            }

            @* Última página *@
            <li class="page-item @(paginaAtual == totalPaginas ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-page="@totalPaginas"
                   asp-route-nome="@ViewBag.NomeFiltro"
                   asp-route-especialidade="@ViewBag.EspecialidadeFiltro"
                   asp-route-ativo="@ViewBag.AtivoFiltro">»</a>
            </li>
        </ul>
    </nav>
}
