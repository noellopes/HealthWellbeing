@model IEnumerable<Room>
@{
    ViewData["Title"] = "Lista de Salas";
}
@if (TempData["Mensagem"] != null)
{
    <div class="alert alert-success">@TempData["Mensagem"]</div>
}


<h1>Lista das Salas</h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Nome</th>
            <th scope="col">Capacidade</th>
            <th scope="col">Especialidade</th>
            <th scope="col">Localização</th>
            <th scope="col">Observação</th>
            <th scope="col">Estado</th>
            <th scope="col" class="text-center">Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Room room in Model)
        {
            <tr>
                <td>@room.RoomId</td>
                <td>@room.Name</td>
                <td>@room.Capacity</td>
                <td>@room.Specialty</td>
                <td>@room.Location</td>
                <td>@room.Notes</td>
                <td>@room.Status</td>
                <td class="d-flex gap-2">
                    <a href="@Url.Action("ReadRoom", "Room", new { id = room.RoomId })" class="btn btn-success w-100 text-center">Ver</a>
                    <a href="@Url.Action("UpdateRoom", "Room", new { id = room.RoomId })" class="btn btn-warning w-100 text-center">Editar</a>
                    <a href="@Url.Action("DeleteRoom", "Room", new { id = room.RoomId })" class="btn btn-danger w-100 text-center">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex flex-column flex-md-row justify-content-between gap-2">
                <a asp-controller="Home" asp-action="Index" class="btn btn-secondary flex-fill me-lg-2">Voltar ao início</a>
                <a asp-controller="Room" asp-action="CreateRoom" class="btn btn-primary flex-fill me-lg-2">Adicionar nova Sala</a>
            </div>
        </div>
    </div>
</div>