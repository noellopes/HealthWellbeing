@model HealthWellbeing.Models.ProblemaSaude

@{
    ViewData["Title"] = "Problema de Saúde Inválido";
}

<h1 class="mb-4">@ViewData["Title"]</h1>

<div class="alert alert-warning d-flex align-items-center p-4 border border-warning rounded shadow-sm" role="alert">
    <i class="bi bi-exclamation-triangle-fill flex-shrink-0 text-warning" style="font-size: 4rem;"></i>
    <div class="ps-4">
        <h4 class="alert-heading mb-2">O registo não existe</h4>
        <p class="mb-0">
            O problema de saúde que tentou editar ou visualizar já não existe na base de dados.
            É possível que tenha sido eliminado por outro utilizador.
        </p>
    </div>
</div>

@if (Model != null)
{
    <div class="card border-info mb-4 shadow-sm">
        <div class="card-header bg-info text-white">
            <i class="bi bi-arrow-repeat me-2"></i> Recuperação de Dados
        </div>
        <div class="card-body">
            <h5 class="card-title">Deseja guardar os dados como um novo registo?</h5>
            <p class="card-text">
                Apesar de o registo original ter sido apagado, ainda temos os dados que acabou de introduzir.
                Pode adicioná-los novamente à base de dados como um novo Problema de Saúde.
            </p>

            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="ProblemaCategoria" />
                <input type="hidden" asp-for="ProblemaNome" />
                <input type="hidden" asp-for="ZonaAtingida" />
                <input type="hidden" asp-for="Gravidade" />

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-plus-circle me-1"></i> Adicionar Novamente
                    </button>

                    <a asp-action="Index" class="btn btn-outline-secondary">
                        Cancelar e Voltar à Lista
                    </a>
                </div>
            </form>
        </div>
    </div>
}
else
{
    <div class="mt-4 text-end">
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle me-1"></i> Voltar à Lista
        </a>
    </div>
}
