@model HealthWellbeing.Models.AuditoriaConsumivel

@{
    ViewData["Title"] = "Editar Auditoria de Consumível";
}

<style>
    .edit-container {
        max-width: 600px;
        margin: 40px auto;
    }

    .card {
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
    }

    .card-header {
        background-color: #0d6efd;
        color: white;
        font-weight: 600;
        font-size: 1.25rem;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
    }

    .form-label {
        font-weight: 500;
    }

    .btn-spacing {
        margin-right: 10px;
    }
</style>

<div class="edit-container">
    <div class="card">
        <div class="card-header">
            ✏️ Editar Auditoria de Consumível
        </div>
        <div class="card-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="AuditoriaConsumivelId" />

                <div class="mb-3">
                    <label asp-for="SalaId" class="form-label"></label>
                    <select asp-for="SalaId" class="form-select" asp-items="ViewBag.SalaId">
                        <option value="">-- Selecione a Sala --</option>
                    </select>
                    <span asp-validation-for="SalaId" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ConsumivelID" class="form-label"></label>
                    <select asp-for="ConsumivelID" class="form-select" asp-items="ViewBag.ConsumivelID">
                        <option value="">-- Selecione o Consumível --</option>
                    </select>
                    <span asp-validation-for="ConsumivelID" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="QuantidadeUsada" class="form-label"></label>
                    <input asp-for="QuantidadeUsada" class="form-control" placeholder="Ex: 10" />
                    <span asp-validation-for="QuantidadeUsada" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DataConsumo" class="form-label"></label>
                    <input asp-for="DataConsumo" type="datetime-local" class="form-control" />
                    <span asp-validation-for="DataConsumo" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary btn-spacing">💾 Guardar alterações</button>
                    <a asp-action="Index" class="btn btn-secondary">⬅️ Voltar</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
