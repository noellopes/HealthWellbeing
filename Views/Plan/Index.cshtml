@model IEnumerable<HealthWellbeing.Models.Plan>

@{
    ViewData["Title"] = "Plan Management";
}

<h1 class="display-4 text-primary mb-4">@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary shadow-sm">
        <i class="bi bi-plus-circle"></i> Create New Plan
    </a>
</p>

<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
                <th class="d-none d-sm-table-cell">
                    @Html.DisplayNameFor(model => model.DurationDays)
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="fw-bold">
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @string.Format("{0:C}", item.Price)
                    </td>
                    <td class="d-none d-sm-table-cell">
                        @Html.DisplayFor(modelItem => item.DurationDays)
                    </td>
                    <td class="text-nowrap">
                        <a asp-action="Edit" asp-route-id="@item.PlanId" class="btn btn-sm btn-outline-primary me-1">
                            Edit
                        </a>
                        <a asp-action="Details" asp-route-id="@item.PlanId" class="btn btn-sm btn-outline-info me-1">
                            Details
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.PlanId" class="btn btn-sm btn-outline-danger">
                            Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
